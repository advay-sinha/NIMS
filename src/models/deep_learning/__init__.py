"""Engine B deep-learning models (PyTorch).

Wrappers implementing the Engine A :class:`src.models.base.BaseModel`
interface, so the existing trainer, metrics and experiment tracking are reused
unchanged. Architectures: MLP, 1D-CNN, LSTM and Transformer over tabular
feature vectors.
"""
