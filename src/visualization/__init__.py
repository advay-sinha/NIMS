"""Visualization artefacts for NetSentinel experiments.

Renders production-ready plots from existing experiment, explainability and
error-analysis artefacts — no predictions, SHAP values or training are ever
recomputed. Artefacts are persisted per experiment under
``outputs/visualizations/<experiment_id>/``.
"""
